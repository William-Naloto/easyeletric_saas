# OS/editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# local secrets
.env
*.env
*.local

# generated archives
*.zip

# Never commit tokens (mas NÃO patches: .patch é convenção do
# git format-patch, usado no fluxo de review desta equipe — *pat*
# batia em qualquer nome contendo "pat", inclusive "*.patch")
*token*
*.pat
*_pat
*_pat.*
github_pat*
!*.patch
